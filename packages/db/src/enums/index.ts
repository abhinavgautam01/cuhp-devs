export enum UserRole {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum SubmissionResult {
  AC = "AC",
  REJECTED = "REJECTED",
  PENDING = "PENDING",
}

export enum Difficulty {
  EASY = "EASY",
  MEDIUM = "MEDIUM",
  HARD = "HARD",
}